apiVersion: v1
kind: Service
metadata:
  name: demo
spec:
  type: NodePort
  selector:
    app: demo
  ports:
    - name: http
      port: 80
      targetPort: http
      # Mapped to host port 18080 by clusters/kind/kind-config.yaml
      nodePort: 30080
