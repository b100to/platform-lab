{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "idle-reaper.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "idle-reaper.resourceName" (dict "suffix" "idlewindow-admin-role" "context" $) }}
rules:
- apiGroups:
  - finops.b100to.dev
  resources:
  - idlewindows
  verbs:
  - '*'
- apiGroups:
  - finops.b100to.dev
  resources:
  - idlewindows/status
  verbs:
  - get
{{- end }}
